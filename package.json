{
  "name": "great-repo-files",
  "private": true,
  "workspaces": [
    "installer-ts"
  ],
  "scripts": {
    "changeset-version": "cd installer-ts && bun run changeset-version",
    "changeset-publish": "cd installer-ts && bun run changeset-publish"
  }
}
